# This is just an example file
# copy this to app.production.yaml and then run `gcloud app deploy app.production.yaml`

runtime: nodejs
env: flex

manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
env_variables:
  TRUSTED_ORIGIN: https://rattle-battle.now.sh
  DB_URL:
  JWT_SECRET:
